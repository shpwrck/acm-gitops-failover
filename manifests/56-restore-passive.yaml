# Passive-hub posture — PASSIVE hub (spoke) only.
# skip = do NOT activate managed clusters here; sync = keep pulling new
# backups as they land. Flipping veleroManagedClustersBackupName to `latest`
# (manifests/57-restore-activate.yaml) IS the failover action.
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Restore
metadata:
  name: restore-acm-passive-sync
  namespace: open-cluster-management-backup
spec:
  veleroManagedClustersBackupName: skip
  veleroCredentialsBackupName: latest
  veleroResourcesBackupName: latest
  cleanupBeforeRestore: CleanupRestored
  syncRestoreWithNewBackups: true
  restoreSyncInterval: 10m
