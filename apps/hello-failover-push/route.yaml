apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: hello-failover-push
spec:
  to:
    kind: Service
    name: hello-failover-push
  port:
    targetPort: 8080
  tls:
    termination: edge
