# DR role for cluster `spoke`. Reconciled by SPOKE'S OWN local Argo CD
# (dr/bootstrap/dr-role-spoke.yaml). See ../README.md for the flip
# choreography.
# CURRENT ROLE: active (Automated Push Path exercise 2026-08-18: PR-A
# activation claimed the fleet; PR-B promotes to full active. Stamped
# one-shot kept — the NEXT demote PR reaps it, V5 housekeeping).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../roles/active
  - restore-activate-202608181929.yaml
