# DR role for cluster `spoke`. Reconciled by SPOKE'S OWN local Argo CD
# (dr/bootstrap/dr-role-spoke.yaml). See ../README.md for the flip
# choreography.
# CURRENT ROLE: ACTIVATING (path-4 PR-A, 2026-08-13 — activation one-shot
# only; promotion PR-B adds ../roles/active after the claim lands).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - restore-activate-202608131843.yaml
