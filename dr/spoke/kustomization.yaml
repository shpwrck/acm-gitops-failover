# DR role for cluster `spoke`. Reconciled by SPOKE'S OWN local Argo CD
# (dr/bootstrap/dr-role-spoke.yaml). See ../README.md for the flip
# choreography.
# CURRENT ROLE: passive (path-2 attempt-1 reverted 2026-08-13 — activation
# restore was ignored while the role's BackupSchedule was active, and the
# premature schedule poisoned the bucket's `latest`; see runbook V1 finding).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../roles/passive
